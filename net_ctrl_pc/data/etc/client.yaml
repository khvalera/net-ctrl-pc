mqtt:
  host: "192.168.1.100"
  port: 1883
  base_topic: "net-ctrl-pc"
  username: "deye"
  password: "Passw"

client:
  id: "pc01"
  exec_timeout: 1

logging:
  file: /var/log/net-ctrl-pc/server.log
  level: INFO
  # size or "time"
  rotation: size
  # 10MB
  max_bytes: 10485760
  backup_count: 5
  # if rotation = "time"
  when: midnight
  # number of intervals (1 day)
  interval: 1
